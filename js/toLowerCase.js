/**
 * This is sort of strange to ask this question in javascript because there is a method with literally the exact 
 * same signature in javascript already
 * @param {string} str
 * @return {string}
 */
var toLowerCase = function(str) {
    return str.toLowerCase();
};